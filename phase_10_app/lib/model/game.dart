// Game configuration model for Phase 10
class Game {
  final int maxRounds;
  final int numPhases;
  final int numPlayers;

  const Game({this.maxRounds = 14, this.numPhases = 10, this.numPlayers = 8});
}
